package com.cortexcrm.entity;

/** Business intent expressed in an activity note, derived by InsightService. */
public enum Intent {
    PRICING,
    COMPLAINT,
    RENEWAL,
    CHURN,
    UPSELL,
    OTHER
}
